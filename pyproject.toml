[project]
name = "3-3-1"
version = "0.1.0"
description = "A simple web applicaation to provide math practice problems."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.3",
    "mathgenerator>=1.5.0",
]
